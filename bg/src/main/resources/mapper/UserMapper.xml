<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartcoffee.mapper.UserMapper">
  <resultMap id="UserMap" type="com.smartcoffee.entity.User">
    <id column="id" property="id"/>
    <result column="username" property="username"/>
    <result column="password" property="password"/>
    <result column="role" property="role"/>
    <result column="phone" property="phone"/>
    <result column="status" property="status"/>
    <result column="create_time" property="createTime"/>
  </resultMap>

  <select id="findById" parameterType="long" resultMap="UserMap">
    SELECT id, username, password, role, phone, status, create_time
    FROM users
    WHERE id = #{id}
  </select>

  <select id="findByUsername" parameterType="string" resultMap="UserMap">
    SELECT id, username, password, role, phone, status, create_time
    FROM users
    WHERE username = #{username}
  </select>

  <select id="list" resultMap="UserMap">
    SELECT id, username, password, role, phone, status, create_time
    FROM users
    ORDER BY id ASC
    <if test="offset != null and limit != null">
      LIMIT #{offset}, #{limit}
    </if>
  </select>

  <select id="count" resultType="int">
    SELECT COUNT(*) FROM users
  </select>

  <insert id="insert" parameterType="com.smartcoffee.entity.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users(username, password, role, phone, status)
    VALUES(#{username}, #{password}, #{role}, #{phone}, #{status})
  </insert>

  <update id="update" parameterType="com.smartcoffee.entity.User">
    UPDATE users
    SET username = #{username},
        password = #{password},
        role = #{role},
        phone = #{phone},
        status = #{status}
    WHERE id = #{id}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM users WHERE id = #{id}
  </delete>

  <update id="updatePassword">
    UPDATE users
    SET password = #{newPassword}
    WHERE id = #{id} AND password = #{oldPassword}
  </update>
</mapper>
