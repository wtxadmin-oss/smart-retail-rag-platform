<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartcoffee.mapper.OrderMapper">
  <resultMap id="OrderMap" type="com.smartcoffee.entity.Order">
    <id column="id" property="id"/>
    <result column="order_no" property="orderNo"/>
    <result column="user_id" property="userId"/>
    <result column="username" property="username"/>
    <result column="store_id" property="storeId"/>
    <result column="store_name" property="storeName"/>
    <result column="total_amount" property="totalAmount"/>
    <result column="status" property="status"/>
    <result column="receiver_name" property="receiverName"/>
    <result column="receiver_phone" property="receiverPhone"/>
    <result column="receiver_address" property="receiverAddress"/>
    <result column="create_time" property="createTime"/>
    <result column="pay_time" property="payTime"/>
  </resultMap>

  <resultMap id="OrderItemMap" type="com.smartcoffee.entity.OrderItem">
    <id column="id" property="id"/>
    <result column="order_id" property="orderId"/>
    <result column="product_id" property="productId"/>
    <result column="product_name" property="productName"/>
    <result column="product_image" property="productImage"/>
    <result column="sku_spec" property="skuSpec"/>
    <result column="price" property="price"/>
    <result column="quantity" property="quantity"/>
  </resultMap>

  <select id="list" resultMap="OrderMap">
    SELECT o.*, u.username, s.name AS store_name
    FROM orders o
    JOIN users u ON o.user_id = u.id
    LEFT JOIN store s ON o.store_id = s.id
    WHERE (#{userId} IS NULL OR o.user_id = #{userId})
      AND (#{status} IS NULL OR o.status = #{status})
    ORDER BY o.create_time DESC
    <if test="offset != null and limit != null">
      LIMIT #{offset}, #{limit}
    </if>
  </select>

  <select id="count" resultType="int">
    SELECT COUNT(*)
    FROM orders
    WHERE (#{userId} IS NULL OR user_id = #{userId})
      AND (#{status} IS NULL OR status = #{status})
  </select>

  <select id="findById" resultMap="OrderMap">
    SELECT o.*, u.username, s.name AS store_name
    FROM orders o
    JOIN users u ON o.user_id = u.id
    LEFT JOIN store s ON o.store_id = s.id
    WHERE o.id = #{id}
  </select>

  <select id="listItemsByOrderId" resultMap="OrderItemMap">
    SELECT * FROM order_item WHERE order_id = #{orderId}
  </select>

  <insert id="insert" parameterType="com.smartcoffee.entity.Order" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO orders(order_no, user_id, store_id, total_amount, status, receiver_name, receiver_phone, receiver_address)
    VALUES(#{orderNo}, #{userId}, #{storeId}, #{totalAmount}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress})
  </insert>

  <insert id="insertItem" parameterType="com.smartcoffee.entity.OrderItem">
    INSERT INTO order_item(order_id, product_id, product_name, product_image, sku_spec, price, quantity)
    VALUES(#{orderId}, #{productId}, #{productName}, #{productImage}, #{skuSpec}, #{price}, #{quantity})
  </insert>

  <update id="updateStatus">
    UPDATE orders SET status = #{status} WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM orders WHERE id = #{id}
  </delete>
</mapper>
