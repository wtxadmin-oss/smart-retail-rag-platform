<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartcoffee.mapper.ProductSkuMapper">
  <select id="listByProductId" parameterType="long" resultType="com.smartcoffee.entity.ProductSku">
    SELECT id, product_id AS productId, spec_name AS specName, price, stock
    FROM product_sku
    WHERE product_id = #{productId}
    ORDER BY price ASC
  </select>
  <select id="findById" parameterType="long" resultType="com.smartcoffee.entity.ProductSku">
    SELECT id, product_id AS productId, spec_name AS specName, price, stock
    FROM product_sku
    WHERE id = #{id}
  </select>
  <insert id="insert" parameterType="com.smartcoffee.entity.ProductSku" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO product_sku(product_id, spec_name, price, stock)
    VALUES(#{productId}, #{specName}, #{price}, #{stock})
  </insert>
  <update id="update" parameterType="com.smartcoffee.entity.ProductSku">
    UPDATE product_sku
    SET product_id = #{productId},
        spec_name = #{specName},
        price = #{price},
        stock = #{stock}
    WHERE id = #{id}
  </update>
  <delete id="deleteById" parameterType="long">
    DELETE FROM product_sku WHERE id = #{id}
  </delete>
</mapper>
